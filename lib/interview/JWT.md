JWT => JSON WEB TOKEN
1. 解决不是浏览器环境的过期时间问题
2. 如何防止令牌被篡改

# 为什么会不是浏览器环境
由于现在大部分应用都是前后端分离，前端的应用环境更加多样，所以需要处理不同环境之间的差异。

1. 浏览器环境
   以前在浏览器环境中，我们通常都会使用cookies保存用户token，因为浏览器环境可以自动存储服务器传过来的cookies，并且根据cookies的内容，如配置中的过期时间，浏览器就会根据这个过期时间自动删除cookies。那么在业务场景中，例如进入首页的时候就可以自动识别当前用户权限，从而决定显示哪些页面。